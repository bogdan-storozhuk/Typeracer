// let array = [];
// class HandlersFactory{
//     createHandler(key, callback){
//         return {key, callback}
//     }
// }

// let handlersFactory = new HandlersFactory();

// array.push(handlersFactory.createHandler('timer',  
// ({countdown}) => {
//     if (!raceFinished) {
//         const timer = document.getElementById('time');
//         timer.textContent = `The next race starts in: ${countdown}`;
//     }
// }));

// array.push(handlersFactory.createHandler('timer'),



// array.forEach(item, index => socket.on(array[index].key, array[index].callback));

